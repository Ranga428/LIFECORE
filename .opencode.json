{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "my-local-ai": {
      "name": "Local Models",
      "options": {
        "baseURL": "http://localhost:12434/v1",
        "apiKey": "${OPENCODE_API_KEY}"
      }
    }
  }
}